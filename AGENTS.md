기본적으로 이 언어는 서브컬처 밈을 위한 언어 림언어이다.

트릭컬 리바이브 "림"의 썰렁 개그를 연장한다.

사칙연산:

더하/더한 등 add에 해당하는 모든 한국어 변용

빼, 뺀, 빼면 등 minus에 해당하는 모든 한국어 변용


python3의 기본 연산 지원,.

...

이런식으로 모든 연산 정의

플립점프의 경우에는...

A 후라이팬에 B를 뒤집으면, 그리고 그에 해당하는 모든 한국어 변용


sbn의 경우에는...

a에서 b를 흘려서 c가 되면, 그리고 그에 해당하는 모든 한국어 변용

한국어 구문 변이 및 파싱은 external libs 써도 됨, c17로 언어 작성.


각 구문은

구문1...?
구문2.

형태의 문답이어야 함


조건 분기는

~~가 ~~하면 형태로 해야 함

## 서브컬쳐 예시 농담

케이크가 지르는 비명은...?
케이크가 비명을 지르면 이크.

> 이 경우, 케이크가 지르는 비명, 케이크가 비명을 지르면 이 둘이 같은 한국어 분석기에 들어가 동일한 뜻으로 파싱된다.
> 문장을 한 번 더 썼기 때문에, 함수에 입력되어 있는 "이크"가 출력된다.

만약 c로 따지면 "케이크가 지르는 비명은" 자체가 `char * func() { return "이크" }` 를 뱉고, 문장의 끝이니 .이 자동 완성된다.

## 예시 2

빵 중에 가장 예의가 없는 빵은?
그건 바로...메론빵....

"그건 바로" 나 동의어는 가장 앞의 함수의 데이터를 즉시 반환해 쓰고, ...을 먼저 쓴 후 종결한다는 뜻이다. 점이 한개 더 붙으니 이 경우 문장 끝 처리랑 합쳐져서 점이 4개가 되나, 구문 중간에서는 3개이다.

따라서 이 formatted print에서 "빵 중에 가장 예의가 없는 빵"과 한국어 동의어는 모두 "메론빵"을 반환하는 다대일 매핑이다.

## 다른 예시들

친구가 없을 때 -> "~ㄹ 때"나 한국어로 동일 구문은 인터프리터에서 앞 함수의 값이 참, 거짓일 때 평가되어 참일 때 실행되는 분기문이다.

따라서 이 경우 "친구가 없"이 참일 경우 분기에 진입한다.

이 분기에서 "낫이 혼자 나의 개그를 들어줬었거든요."

이것은 C로는 `a->alone == true && a == listen_t`로 a가 type_t를 가진다는 것을 뜻한다.

그리고 이 뒤에 이어지는 구문은 한국어 표준 영어 표기<->영어 간의 치환이다.

"그러니까 이 낫은 낫 저스트 어 낫...후후후...."

는 "그러니까 이 낫은 낫 just a 낫...후후후...."로 외국어로 강하게 의심되는 구간을 영어로 바꿔도 동일하게 해석되어야 한다.

한국어<->영어는 기본 논리 지시에 대해서는 양쪽 치환표를 하드코딩한다.


에서 `a`가 단순히 a라는 변수가 아닌. `listen_t`를 가졌기 때문에 (void *)가 아닌 구체 타입이 있는 변수임을 뜻한다.

따라서 이것이 통째로 assert 문으로 평해지고, ...은 assert 다음에 출력해야 할 조건을 뜻한다.

이 때 후후후 함수가 ..을 출력하게 하고, 끝에 .이 완성되어서

> 친구가 없을 때 낫이 혼자 나의 개그를 들어줬었거든요.
> 그러니까 이 낫은 낫 저스트 어 낫...후후후....


가 원본이다.

마찬가지로, ~니까 타입 역시 분기문이다.

"이제 그 냉각기가 있으니까...난 이제 냉각기로도 무쓸모가 된 거잖아요..."

이 경우에서, 이제는 "실시간 처리되어야 함"을 뜻하고, "있으니까"는 t/f 불리언 판정이다.

"림은 이제 냉각기로도 필요가 없으면...그럼...림은...이제 냉.무.인거에요...푸훗."

이런 구문의 경우, ~인거에요 같은 종결어미를 파싱해 문장 종결로 치환해 파싱에서 제외한다(즉 오류 구문은 모두 종결로 간주해 어떻게든 실행되게 한다)

그리고, "냉.무" 같은 것은 객체 "냉각기"의 첫 글자를 따고, 이후 .무, 혹은 .유를 붙여서 t/f 판별 구문으로 쓸 수 있게 한 폼이다.


삼항연산자.

"해골 가면을 쓴 림과, 가면을 안 쓴 림으로 나누어 게임을 했지요."

이대로는 평가가 안되는 무효 구문이다.

"가면을 쓴 림은 바로 낯.가.림.입니다. 푸흡..."

이라고 삼항 조건을 채우고, "푸흡..."을 넣으면, 삼항 조건이 완성된다.

통모짜핫도그의 반대말은?
요즘잘자쿨냥이. 풉. 푸흐흐흐....

> 통모짜핫도그를 입력받으면 항상 "요즘잘자쿨냥이. 풉. 푸흐흐흐..."를 출력하는 함수 "반대말"이다.
