# RimLang (림언어) 프로젝트 계획 및 명세서

## 1. 개요
RimLang은 트릭컬 리바이브의 캐릭터 '림'의 썰렁 개그 및 언어유희 밈을 바탕으로 하는 C17 기반의 난해/유머러스 프로그래밍 언어입니다.
문답형 구문(`구문1...?` -> `구문2.`), 한국어 어미 변용 파싱, 삼항/조건 분기, OISC(SBN, FlipJump) 및 사칙연산 기반의 튜링 완전성, POSIX 멀티프로세스 병렬/비동기 평가 엔진을 갖춘 실행기를 구현합니다.

## 2. 언어 명세 및 문법 규칙

### 2.1 기본 문답 구조
- 모든 문장은 기본적으로 `구문1...?` (질문/전제) 와 `구문2.` (응답/실행) 형태의 문답 쌍으로 구성됩니다.
- 문장 종결 및 말줄임표 처리:
  - `...?` : 질문/전제 종결자
  - `.` / `....` / `푸훗.` / `푸흡...` / `후후후....` : 응답 및 평가 종결자

### 2.2 튜링 완전 연산 및 OISC 지원
1. **사칙연산 및 기본 제어**:
   - 덧셈: `A에 B를 더하면...` -> `A + B`
   - 뺄셈: `A에서 B를 빼면...` -> `A - B`
   - 곱셈: `A에 B를 곱하면...` -> `A * B`
   - 나눗셈: `A를 B로 나누면...` -> `A / B`
2. **SBN (Subtract and Branch if Negative / Zero)**:
   - `a에서 b를 흘려서 c가 되면...`
   - 동작: `mem[a] = mem[a] - mem[b]; if (mem[a] <= 0) pc = c;`
3. **플립점프 (FlipJump)**:
   - `A 후라이팬에 B를 뒤집으면 C...`
   - 동작: `mem[A] ^= (1 << B); pc = C;`
4. **대입 / 값 설정**:
   - `A는 B...` / `A를 B로 채우면...`

### 2.3 림 전용 개그 구문 및 내장 매핑
- **자명한 썰렁 개그 / 다대일 매핑 함수**:
  - `케이크가 지르는 비명은...? / 케이크가 비명을 지르면 이크.` -> `"이크"`
  - `빵 중에 가장 예의가 없는 빵은...? / 그건 바로...메론빵....` -> `"메론빵"`
  - `통모짜핫도그의 반대말은...? / 요즘잘자쿨냥이. 풉. 푸흐흐흐....` -> `"요즘잘자쿨냥이"`
  - `그건 바로...` / `바로...` : 직전 평가 값 즉시 반환/출력

### 2.4 조건 분기 및 삼항 연산
- **조건문 (`~ㄹ 때`, `~면`, `~니까`)**:
  - `A가 참일 때 ...` / `A가 B하면 ...` / `A가 있으니까 ...`
- **T/F 축약자 (`.유`, `.무`)**:
  - `냉.무` -> `냉각기 == 0` (False)
  - `냉.유` -> `냉각기 != 0` (True)
- **삼항 연산자 (`낯.가.림`)**:
  - 전제: `X와 Y로 나누어 게임을 했지요.`
  - 분기/선택: `조건인 림은 바로 낯.가.림.입니다. 푸흡...` -> Condition ? X : Y

## 3. 멀티프로세스 비동기/병렬 평가 엔진 아키텍처
- `fork()` 및 POSIX 파이프 / 공유 메모리 기반 프로세스 풀 워커.
- 독립된 문답 구문 및 표현식 분산 평가 및 병렬화.
- `이제` 키워드 감지 시 실시간 동기화 플러시.

## 4. 디렉터리 및 모듈 구조

```
rimlang/
├── CMakeLists.txt
├── Makefile
├── PROJECT.md
├── AGENTS.md
├── include/
│   ├── rim/
│   │   ├── common.h          # 공통 정의, 토큰/타입 정의
│   │   ├── ast.h             # AST 노드
│   │   ├── korean_synonym.h  # 한국어 어미/동의어 정규화
│   │   ├── lexer.h           # 렉서
│   │   ├── parser.h          # 파서
│   │   ├── runtime.h         # VM & OISC & 메모리
│   │   ├── engine.h          # 멀티프로세스 워커 풀 엔진
│   │   └── joke_table.h      # 서브컬처 개그 테이블
├── src/
│   ├── main.c                # 메인 엔트리포인트
│   ├── parser/
│   │   ├── lexer.c
│   │   ├── parser.c
│   │   └── korean_synonym.c
│   ├── engine/
│   │   ├── engine.c          # 멀티프로세스 평가 및 IPC
│   ├── runtime/
│   │   ├── runtime.c         # VM 실행 및 OISC 연산
│   │   └── joke_table.c      # 개그 및 언어 치환
└── tests/
    ├── test_joke.rim
    ├── test_sbn_loop.rim
    ├── test_parallel.rim
    └── test_runner.c
```
