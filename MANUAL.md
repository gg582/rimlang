# RimLang (림언어) 개발자 레퍼런스 매뉴얼 & 아키텍처

RimLang은 트릭컬 리바이브의 사도 **림(Rim)**의 썰렁 개그, 언어유희 및 서브컬처 밈 구문을 바탕으로 동작하는 C17 기반의 난해(Esoteric) 프로그래밍 언어입니다.  
오픈소스 한국어 형태소 분석기 **Kiwi (kiwipiepy)** 와 경량 1B LLM(**Llama 3.2 1B**)이 결합된 하이브리드 엔진을 탑재하고 있습니다.

---

## 1. 아키텍처 및 동작 원리

림언어는 결정론적 문법 해석 계층과 신경망 추론 계층이 분리되어 동작합니다.

```
                      [ RimLang 프로그램 / REPL 입력 ]
                                     │
                                     ▼
        ┌────────────────────────────────────────────────────────┐
        │  1. 결정론적 언어 코어 (C17 AST Engine & Kiwi NLP)     │
        ├────────────────────────────────────────────────────────┤
        │ • 튜링 완전 OISC 연산 (SBN, FlipJump)                  │
        │ • 표준 I/O 함수 (`림...` 출력, `흡...` 입력)           │
        │ • 산술 연산자 (+, -, *, /) 및 변수 바인딩             │
        │ • 실시간 불리언 판정 및 상태 축약자 (.유 / .무)         │
        │ • 삼항 조건 분기 (해골 가면 림 vs 안 쓴 림)            │
        │ • 타입 단언 (assert: not just a not)                  │
        │ • 비명 서브스트링 연산식 (cake -> '이크', 수박 -> '박')│
        │ • 한영 표기 디코딩 및 사투리 음운 변동 정규화         │
        └────────────────────────────┬───────────────────────────┘
                                     │
                         (창작 / 열린 연상 질의)
                                     │
                                     ▼
        ┌────────────────────────────────────────────────────────┐
        │  2. 신경망 의미 오라클 (Llama 3.2 1B via Ollama)       │
        ├────────────────────────────────────────────────────────┤
        │ • 기계적 규칙이 없는 창작 썰렁개그 실시간 추론         │
        │ • 자유 대화 및 열린 의미 질문 처리                    │
        └────────────────────────────────────────────────────────┘
```

---

## 2. 공식 예제 목록

### 예제 1: 원작 100% 반영 썰렁개그 ([examples/trickcal_rim_sum_squares.rim](file:///home/yjlee/rimlang/examples/trickcal_rim_sum_squares.rim))
```rimlang
# 1. 비명 소리 추출 함수식
케이크가 지르는 비명은...?

# 2. 다대일 매핑 및 즉시 반환 함수식 ("그건 바로... ....")
빵 중에 가장 예의가 없는 빵은?

# 3. 실시간 지시어("이제")와 불리언 판정("있으니까") 및 객체 축약자("냉.무")
이제 그 냉각기가 있으니까...난 이제 냉각기로도 무쓸모가 된 거잖아요...?

# 4. 삼항연산자 조건 완성 분기식 (낯.가.림)
해골 가면을 쓴 림과, 가면을 안 쓴 림으로 나누어 게임을 했지요...?

# 5. 아티팩트 타입 단언 (assert & not just a not)
친구가 없을 때 낫이 혼자 나의 개그를 들어줬었거든요...?

# 6. 반대말 함수 평가식 (NOT 부정 연산)
통모짜핫도그의 반대말은?
```

### 예제 2: 창작 썰렁개그 및 방언 호환 ([examples/creative_rim_jokes.rim](file:///home/yjlee/rimlang/examples/creative_rim_jokes.rim))
```rimlang
# 1. 음운 치환 및 언어유희 함수 호출 (banana laugh -> 바나나킥)
바나나가 웃으면...?

# 2. 비교 연산 및 객체 명명 함수 호출 (car hit person -> 카놀라유)
차가 지나가다 사람을 치면...?

# 3. 작별 인사 결합 함수 호출 (King + Bye -> 바이킹)
왕이 헤어질 때 하는 인사는...?

# 4. 방언(사투리) 음운 축약 질의 (동남 방언 '비맹')
케익이 지르는 비맹은...?

# 5. 방언(사투리) 조사 변용 질의 (서남 방언 '싸가지 엄는 빤')
싸가지 엄는 빤은?
```

### 예제 3: 튜링 완전 OISC + 표준 I/O + LLM 파이프라인 ([examples/turing_io_llm_pipeline.rim](file:///home/yjlee/rimlang/examples/turing_io_llm_pipeline.rim))
```rimlang
# 1. 튜링 완전 OISC(SBN) 메모리 연산으로 상태 계산
10에서 4를 흘려서 0이 되면...

# 2. 림언어 표준 출력 함수 ("림...")
림... 오늘의 퀴즈를 시작하겠습니다!

# 3. 비명 서브스트링 연산식 평가
케이크가 지르는 비명은...?

# 4. 언어유희 및 창작 질문식 평가
바나나가 웃으면...?
```

---

## 3. 빌드 및 테스트 가이드

```bash
# 1. 자동 설치
./install.sh

# 2. 수동 컴파일
make all

# 3. 예제 실행
./rimlang examples/trickcal_rim_sum_squares.rim
./rimlang examples/creative_rim_jokes.rim
./rimlang examples/turing_io_llm_pipeline.rim

# 4. 데모 GIF 재생성
./util/make_gif.sh
```

---

## 6. 결정론적 프로그램 블록 및 LLM 차단 가드 블록

RimLang은 순수 REPL이나 스크립트 작성 시, 특정 구역의 연산을 결정론적으로 격리하거나 LLM의 개입을 원천 차단하기 위한 **블록 스코프 구문**을 제공합니다.

### 블록 키워드 규약
1. **`교주님...` ~ `흡...흐흡...` (결정론적 프로그램 블록)**:
   - 교주님 블록 내부는 튜링 완전 산술식 및 OISC 메모리 제어식을 연속적으로 작성하여 실행합니다.
2. **`크흡...` ~ `흡...흐흡...` (LLM 평가 가드 블록)**:
   - 가드 블록 내부에 작성된 모든 구문은 **LLM 신경망 오라클의 평가가 100% 차단**되며, 순수 C17 런타임과 Kiwi 형태소 분석기로만 결정론적 평가를 수행합니다.

### 가드 블록 예제 ([examples/guarded_program_block.rim](file:///home/yjlee/rimlang/examples/guarded_program_block.rim))
```rimlang
# 1. 결정론적 프로그램 실행 블록 ("교주님..." ~ "흡...흐흡...")
교주님...
10에서 4를 흘려서 0이 되면...
림... 교주님, 림의 결산이 완료되었습니다!
흡...흐흡...

# 2. LLM 평가 완전 차단 가드 블록 ("크흡..." ~ "흡...흐흡...")
크흡...
케이크가 지르는 비명은...?
바나나가 웃으면...?
흡...흐흡...
```

---

## 7. 미세 논리 분해 및 사용자 함수 정의 (`림하하...` ~ `하하..람...`)

림언어는 단순 통짜 문장 매핑 대신, **단어 수준의 미세 논리 분해(Sub-word Decomposition)**와 **사용자 정의 함수(Custom Functor)** 구문을 지원합니다.

### 1. 사용자 함수 정의 블록
특정 단어나 구문을 함수로 선언하여, 호출 시 항상 내부의 튜링/결정론적 연산 결과를 반환하도록 바인딩합니다.

```rimlang
림하하...
바나나
# 블록 내부의 튜링/언어유희 구문
림... 바나나킥. 푸하하....
하하..람...

# 호출 시 즉시 치환되어 평가
바나나
```

### 2. 미세 논리 연산자 및 구분자
1. **합성어 세부 형태소 치환**:
   - `통모짜핫도그` $\rightarrow$ `통모짜` (요즘잘자) + `핫` (쿨) + `도그` (냥이) $\rightarrow$ `요즘잘자쿨냥이. 풉. 푸흐흐흐....`
2. **첫 글자 추출 접두 구분자 (`.....`)**:
   - `.....냉각기` $\rightarrow$ 첫 글자 `'냉'` 추출 $\rightarrow$ `'냉.무인거에요...푸훗.'`
